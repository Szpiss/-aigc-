# 首页数据同步修复记录

## 问题

学习或对战结束后，首页偶尔仍显示旧的词力值、总局数或胜利次数。

## 原因

用户数据存在于云数据库、全局状态、页面 store 和本地缓存中。部分流程只更新了其中一处，返回首页时读取到旧值。

## 处理

- 首页 `onShow` 重新拉取用户信息。
- 学习结束后同步词力值和经验值。
- 对战结束后同步总局数、胜利次数和词力值。
- 统一 `totalGames`、`winGames`、`wins` 等字段映射。
- 同步更新 `app.globalData`、store 和本地缓存。

## 检查项

1. 完成一次学习后返回首页，词力值能刷新。
2. 完成一次对战后返回首页，总局数和胜利次数能刷新。
3. 重新打开小程序后用户信息仍能恢复。
4. 数据看板与首页显示一致。
