# 词魂项目测试清单

## 测试目标

对词魂小程序进行验收前检查，覆盖启动、页面、学习、对战、数据写入、异常场景和发布前准备。

## 环境

| 项目 | 内容 |
| --- | --- |
| 项目类型 | 微信小程序 |
| 前端 | 微信小程序原生框架、TypeScript、WXML、WXSS |
| 后端 | 微信云开发、云函数、云数据库 |
| 小程序目录 | `miniprogram/` |
| 云函数目录 | `cloudfunctions/` |

## 准备

- 微信开发者工具可打开项目。
- 小程序 npm 已构建。
- `cloudfunctions/server` 已部署。
- 核心集合可访问：`user`、`book`、`word`、`userWord`、`combatRecord`、`learningRecord`、`learningData`、`wordMastery`、`learningPlan`。
- 准备新用户、普通用户和有历史数据的测试账号。

## 页面检查

| 模块 | 检查项 |
| --- | --- |
| 启动 | 冷启动无白屏，首页可进入 |
| 首页 | 用户信息、词书、功能入口和更多服务显示正常 |
| 词汇学习 | 日常练习、词汇检测、选项练习、认识/不认识流程可用 |
| 单词对战 | 随机匹配、好友对战、人机对战可进入 |
| 生词本 | 空状态、列表、删除和发音可用 |
| 数据看板 | 学习统计、弱词榜、对战统计和最近记录可显示 |
| 排行榜 | 榜单、当前用户区域和切换项可显示 |
| 设置 | 题量、声音、发音、震动、清理数据等配置可用 |
| 学习助手 | 学习建议和对战复盘入口可用 |

## 核心流程

1. 新用户首次进入，默认数据不出现 `undefined` 或 `NaN`。
2. 完成 5 词、25 词、50 词、100 词练习。
3. 词汇检测达到错误上限后能结束。
4. 错词和“不认识”词能进入后续复习数据。
5. 完成人机对战后进入结算页。
6. 对战记录、总局数、胜利次数和词力值能更新。
7. 数据看板能读取学习和对战结果。
8. 学习助手能结合学习或对战数据生成内容。

## 异常场景

- 词库不足时提示实际可用词数。
- 网络异常时有提示，不出现页面卡死。
- 老数据缺少新字段时统计仍能展示。
- 小屏设备下顶部栏、按钮、卡片不遮挡。
- 快速点击入口不重复创建异常房间或重复提交记录。

## 发布前检查

- `git diff --check` 通过。
- 依赖完整环境中重新执行 `npm run lint`。
- 微信开发者工具中重新编译并预览主要页面。
- 云函数部署后完成一次学习和一次对战实测。
