# 创新点分析与新增创新点技术方案

**项目背景**：基于微信小程序的词汇学习与对战系统，包含学习记录、对战记录、统计可视化、AIGC 学习助手。

---

## 一、现有项目的可用创新点（可用于论文阐述）

### 1. 学习-对战数据闭环（弱到中等创新）
- **特点**：学习、对战、日统计形成数据闭环，能支撑学习分析与反馈。
- **论文表述**：提出“学习-对战-反馈”的数据驱动式学习闭环模型。
- **不足**：行业中同类应用较多，创新度偏应用层。

### 2. AIGC 学习助手 + 游戏化对战（中等创新）
- **特点**：将对话式 AI 学习助手与对战学习融合。
- **论文表述**：在小程序场景中实现“对话式辅导+对战练习”的混合学习模式。
- **不足**：AIGC 应用已普遍，需要更明确的独特技术点支撑。

### 3. 云函数低成本学习数据统计（弱创新）
- **特点**：轻量云函数 + 云数据库的统计方案。
- **论文表述**：轻量化学习数据采集与分析在小程序端的实现路径。
- **不足**：偏工程实现，难支撑“创新点”。

---

## 二、建议新增的“更有论文价值”的创新点

### 创新点名称：**对战驱动的个性化词汇调度（Combat-Driven Adaptive Scheduler, CDS）**

**核心思想**：
将用户在“学习模式 + 对战模式”中的错词与反应时长，统一建模为“掌握度评分”，通过自适应调度算法动态生成个性化学习序列，使学习内容随对战表现实时更新。

**创新价值**：
- 将“对战数据”用于学习路径优化，而非仅统计展示。
- 实现“竞技反馈 → 个性化学习 → 竞技表现提升”的闭环优化。
- 适合学术论文中的算法与系统结合叙述。

---

# 新创新点技术文档（CDS 自适应词汇调度）

## 1. 目标
通过对战与学习数据，动态调整每日学习的词汇集合，提高学习效率与对战表现。

## 2. 数据来源
- `combatRecord`：对战错词、是否提示、得分、反应时间（可扩展）
- `learningRecord`：学习错词、学习时长、正确率
- `learningData`：日统计（用于宏观趋势）

## 3. 关键指标定义
为每个词建立一个 **掌握度分数 masteryScore**：

```
masteryScore = w1 * (1 - wrongRate) + w2 * (1 - tipRate) + w3 * responseScore + w4 * recencyScore
```
- **wrongRate**：该词最近 N 次出现的错误率
- **tipRate**：提示使用率（越高表示掌握度越低）
- **responseScore**：响应时间归一化（越慢分越低）
- **recencyScore**：最近出现时间衰减系数（越久越低）

## 4. 调度策略
每日学习任务从三类词汇构成：
- **补弱词 (50%)**：masteryScore 最低的词
- **巩固词 (30%)**：中等掌握度、近期对战出现频繁
- **新词 (20%)**：尚未出现或出现次数少的词

## 5. 调度流程

1. 拉取近 7 天 `combatRecord` + `learningRecord`
2. 聚合到单词级别统计
3. 计算 masteryScore
4. 生成学习列表（按比例抽样）
5. 写入 `learningPlan` 集合（新增）

## 6. 数据结构（新增集合）

### `wordMastery`
```
{
  _openid,
  wordId,
  wrongCount,
  totalCount,
  tipCount,
  avgResponseTime,
  lastSeen,
  masteryScore
}
```

### `learningPlan`
```
{
  _openid,
  date,
  words: [wordId],
  source: { weak: [], reinforce: [], new: [] }
}
```

## 7. 云函数设计
新增云函数方法（在 `learningData` 控制器中扩展）：

- `learningData/generateLearningPlan`
  - 输入：日期 / 目标数量 / 可选词书
  - 输出：学习计划

- `learningData/getLearningPlan`
  - 输入：日期
  - 输出：当日学习计划

## 8. 前端落地
- **学习页**：优先从 `learningPlan` 拉取词列表
- **对战页**：收集错词与提示信息，异步上报
- **统计页**：展示“掌握度分布”和“弱词列表”

## 9. 评估指标（论文可用）
- 学习正确率提升比例
- 对战胜率提升比例
- 重复错误词减少比例
- 平均响应时间降低比例

## 10. 开发步骤建议
1. 增加 `wordMastery` 与 `learningPlan` 集合
2. 实现云函数聚合统计与计划生成
3. 前端学习流程接入计划
4. 统计页新增掌握度视图
5. 对照实验：随机词表 vs 自适应调度

---

**备注**：该创新点可用于论文“算法 + 系统”的综合描述，创新性强于纯功能叠加。
