# 项目技术与架构文档（总览版）

**项目名称**：词魂 - 英语学习与对战小程序（含学习助手）
**文档目的**：梳理系统架构、关键业务逻辑、创新点落地与接口规范，支撑论文与后续维护。  
**版本日期**：2026-01-27

---

## 一、项目总体架构

### 1.1 架构分层
- **小程序前端层**：页面 + 组件 + 状态管理（wxministore）
- **业务逻辑层**：学习/对战流程、数据采集、学习助手入口
- **云函数层**：统一 API 路由（Router）+ 业务控制器
- **数据层（云数据库）**：学习、对战、统计、掌握度、学习计划

### 1.2 核心技术栈
- 微信小程序（TypeScript/JS + WXML/WXSS）
- 云开发 CloudBase（云函数 + 云数据库）
- 聊天组件（腾讯云开发）

---

## 二、核心功能模块与实现要点

### 2.1 单词对战模块
**功能**：随机匹配 / 好友对战 / 人机对战

**实现要点**：
- 房间创建与状态流转：`create → ready → start → end`
- 随机匹配采用预创建与锁定策略，避免并发房间冲突
- 人机对战由随机房间转为 `type: npc` 后自动开启

**关键文件**：
- `miniprogram/pages/combat/*`
- `miniprogram/models/combat.ts`
- `miniprogram/pages/combatSelect/*`

---

### 2.2 词汇学习模块
**功能**：学习题目生成、答题、计时、结算

**实现要点**：
- 学习开始时间写入 `app.learningStartTime`
- 结束时调用 `recordLearningData` 写入学习记录
- 与学习计划联动（优先使用计划词表）

**关键文件**：
- `miniprogram/pages/learning/*`
- `miniprogram/utils/learningDataRecorder.ts`

---

### 2.3 数据采集与统计
**功能**：对战/学习数据记录、日统计汇总

**实现要点**：
- `combatRecord`：对战明细
- `learningRecord`：每日学习明细
- `learningData`：日汇总（upsertDaily）

**关键文件**：
- `cloudfunctions/server/controller/learningData.js`
- `cloudfunctions/server/model/*`

---

### 2.4 数据看板（统计可视化）
**功能**：学习趋势、对战统计、弱词榜

**实现要点**：
- 学习数据来自 `getLearningReport`（周）
- 对战数据来自 `combatRecord`
- 弱词榜来自 `getWordMasteryTop`

**关键文件**：
- `miniprogram/pages/statistics/*`

---

### 2.5 创新点：CDS 自适应词汇调度
**功能**：词汇练习 / 对战错词 → 掌握度更新 → 学习计划生成

**核心流程**：
1. 对战记录写入 `combatRecord`
2. 词汇练习写入 `learningRecord / learningData`
3. 已练单词与错词共同更新 `wordMastery`
4. 生成 `learningPlan`
5. 学习页读取计划词表

**关键集合**：
- `wordMastery`
- `learningPlan`

---

### 2.6 创新点：学习闭环
**功能**：学习助手解释学习计划 + 整理对战复盘

**实现要点**：
- 学习助手页按钮生成结构化 context text
- 自动拼接：学习计划词汇、弱词榜、学习周报、对战摘要
- 发送给聊天组件（聊天窗口）

**关键文件**：
- 学习助手页面脚本
- `miniprogram/components/聊天组件/*`

---

## 三、数据模型设计（核心集合）

### 3.1 combatRecord（对战记录）
```json
{
  _openid,
  combatId,
  combatType,
  bookId,
  bookName,
  isWin,
  score,
  opponentScore,
  totalQuestions,
  correctCount,
  wrongCount,
  tipCount,
  avgResponseTime,
  wrongWords,
  startTime,
  endTime,
  duration,
  _createTime
}
```

### 3.2 learningRecord（学习记录）
```json
{
  _openid,
  bookId,
  bookName,
  score,
  maxScore,
  wordsCount,
  correctCount,
  wrongCount,
  tipCount,
  reviveUsed,
  wrongWords,
  startTime,
  endTime,
  duration,
  _createTime
}
```

### 3.3 learningData（日统计）
```json
{
  _openid,
  date,
  week, month, year,
  totalStudyTime,
  combatStudyTime,
  learningStudyTime,
  combatCount,
  learningCount,
  totalWordsCount,
  correctCount,
  wrongCount,
  tipCount,
  correctRate,
  winCount,
  loseCount,
  avgScore,
  newWordsCount,
  _createTime,
  _updateTime
}
```

### 3.4 wordMastery（掌握度）
```json
{
  _openid,
  wordId,
  word,
  bookId,
  wrongCount,
  totalCount,
  tipCount,
  avgResponseTime,
  masteryScore,
  lastSeen,
  _createTime,
  _updateTime
}
```

### 3.5 learningPlan（学习计划）
```json
{
  _openid,
  date,
  bookId,
  words: [wordId],
  total,
  source: { weak: [], reinforce: [], new: [] },
  _createTime,
  _updateTime
}
```

---

## 四、关键业务流程说明

### 4.1 对战数据采集
1. 对战结束触发 `recordCombatData`
2. 云函数写入 `combatRecord`
3. 同步更新 `learningData`
4. 更新 `wordMastery`

### 4.2 学习数据采集
1. 学习开始保存 startTime
2. 本次练习 / 检测结束触发 `recordLearningData`
3. 云函数写入 `learningRecord`
4. 同步更新 `learningData`
5. 通过 `learnedWords` 更新 `wordMastery`，正确词增加 totalCount，错词同步增加 wrongCount

### 4.3 学习计划生成
1. 读取 `wordMastery`（弱词优先）
2. 补充新词（随机采样）
3. 写入 `learningPlan`
4. 学习页读取计划词表

### 4.4 学习助手复盘/建议生成
1. 拉取 learningPlan + weakWords + learningReport + combatSummary
2. 组织上下文文本
3. 发送到聊天组件

---

## 五、接口文档（云函数）

### 5.1 记录类接口

#### learningData/recordCombat
- 入参：对战统计字段（详见 combatRecord）
- 出参：`{ state: 0 | -1, data }`

#### learningData/recordLearning
- 入参：学习统计字段（详见 learningRecord）
- 出参：`{ state: 0 | -1, data }`

---

### 5.2 统计类接口

#### learningData/getLearningReport
- 入参：`type=week|month|day, date=ISO`
- 出参：`{ daily: [], summary: {} }`

#### learningData/getLearningTrend
- 入参：`days=number`
- 出参：`{ dates, studyTimes, correctRates, wordCounts }`

---

### 5.3 CDS/学习闭环接口

#### learningData/generateLearningPlan
- 入参：`date, size, bookId`
- 出参：`learningPlan`

#### learningData/getLearningPlan
- 入参：`date, bookId`
- 出参：`learningPlan | null`

#### learningData/getWordMasteryTop
- 入参：`limit, bookId`
- 出参：`wordMastery[]`

---

## 六、前端关键调用点

- `recordCombatData`：`settleScene.ts` 内对战结束调用
- `recordLearningData`：`problem.ts` 内学习结束调用
- `getLearningReport`：`statistics.ts`
- `getLearningPlan` / `generateLearningPlan`：`learning.ts`
- 学习助手复盘/建议：学习助手页面脚本

---

## 七、部署与配置要点

1) 云数据库集合必须存在：
- `combatRecord` / `learningRecord` / `learningData`
- `wordMastery` / `learningPlan`

2) 小程序 request 合法域名：
- 若启用发音：`dict.youdao.com`

3) 云函数部署：
- `cloudfunctions/server` 需重新上传并部署

---

## 八、当前完成状态

- ✅ 对战/学习数据采集完整
- ✅ 数据看板可视化
- ✅ CDS 自适应调度落地
- ✅ 学习闭环口径已统一
- ✅ 单词发音功能可用

---

## 九、可选扩展

- 学习计划 A/B 实验统计
- 掌握度分布图 / 趋势图
- 学习助手输出建议写回数据库
- 对话内容纳入掌握度更新

---

**文档整合来源**：
- `创新点与技术方案.md`
- `学习助手融合创新点评估与预测.md`
- `项目改造与修复记录.md`
