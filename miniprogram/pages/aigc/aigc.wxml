<view class="page">
  <view class="back" catchtap="onBack">
    <image class="back-icon" src="./../../images/combat-back.png" />
    <text class="back-text">返回首页</text>
  </view>

  <agent-ui agentConfig="{{agentConfig}}" showBotAvatar="{{showBotAvatar}}" chatMode="{{chatMode}}" modelConfig="{{modelConfig}}" envShareConfig="{{envShareConfig}}">
  </agent-ui>
</view>
