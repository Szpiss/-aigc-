# 项目改造与修复记录
---

## 一、定位与修复：云函数 `TypeError: undefined is not a promise`

### 1.1 现象
- 云函数 `learningData/recordCombat` 报错：`TypeError: undefined is not a promise`
- 堆栈指向 `controller/learningData.js` 与 `model/learningData.js` 的 `__awaiter`

### 1.2 关键修复
- 修复 `combatRecord.js / learningRecord.js / learningData.js` 缺失的 `__assign`，避免运行时崩溃。
- 修复 `controller/learningData.js` 中 `try/catch` 生成器标签与变量缺失。
- 将 `controller/learningData.js` 的 `recordCombat / recordLearning` 改为原生 `async/await`，规避 `__awaiter` 兼容性问题。
- 将 `model/learningData.js` 的 `upsertDaily / getWeekData / getMonthData` 改为原生 `async/await`。

### 1.3 结果
- `combatRecord` 可正常写入
- `learningRecord` 可正常写入
- `learningData` 通过 `upsertDaily` 正常写入

---

## 二、数据库与权限问题修复

### 2.1 现象
- `collection.add:fail -502005 database collection not exists`

### 2.2 处理
- 明确要求创建集合：`combatRecord / learningRecord / learningData`
- 建议测试期权限为“所有用户可读写”

---

## 三、学习数据记录流程补齐

### 3.1 现象
- 学习结束后未写入 `learningRecord / learningData`

### 3.2 处理
- 在 `miniprogram/pages/learning/components/problem/problem.ts` 中
  - 学习结束（生命值归零）时调用 `recordLearningData`
- 开始时间通过 `app.learningStartTime` 存储
  - `learning.ts` 中设置开始时间
  - `problem.ts` 中读取并清空
- 避免使用 `global`，修复 TS 错误

---

## 四、对战数据记录与类型错误修复

### 4.1 现象
- `ServerDate` 类型与 `Date` 构造不兼容
- `record.isTip` 类型不存在
- `combatId` 类型不匹配

### 4.2 处理
- `settleScene.ts`：
  - `ServerDate | string` → 安全转 `Date`
  - `record` 做类型断言并设置默认值
  - `combatId` 强制转为 `string`

---

## 五、数据分析页面建设（新增页面）

### 5.1 页面新增
- 新增页面：`pages/statistics/`
- 展示内容：
  - 学习汇总（时长/词数/正确率/学习次数）
  - 近 7 天学习时长柱状图
  - 每日学习概览列表
  - 对战汇总（场次/胜率/平均得分/平均耗时）
  - 最近 10 场对战记录

### 5.2 接口/数据来源
- 学习数据：云函数 `learningData/getLearningReport`
- 对战记录：直接查询 `combatRecord` 集合

### 5.3 相关改动
- `app.json` 注册统计页面
- `utils/routes.ts` 添加 `/statistics`
- 首页增加“数据看板”入口

---

## 六、首页与对战入口重构

### 6.1 目标
- 将“随机匹配/好友对战”从首页移除
- 新增“单词对战”入口页，包含随机/好友/人机三种入口

### 6.2 新增页面
- `pages/combatSelect/`
  - 3 个按钮：随机匹配 / 好友对战 / 人机对战

### 6.3 逻辑调整
- 首页：新增“单词对战”入口按钮
- `combat.ts` 支持 `type === 'npc'` 的入口，自动创建房间并触发人机
- `routes.ts` 支持 `combatSelect` 路由

---

## 七、编译问题修复（BOM）

### 7.1 现象
- JSON/WXSS 文件报错：`Unexpected token \uFEFF` 或 `unexpected �`

### 7.2 处理
- 将相关文件强制写入为 **UTF-8 无 BOM**：
  - `statistics.json`
  - `statistics.wxss`
  - `combatSelect` 页面文件

---

## 八、当前状态（已完成）

- ✅ 对战数据写入正常（combatRecord）
- ✅ 学习数据写入正常（learningRecord / learningData）
- ✅ 数据分析页面可用
- ✅ 首页对战入口改造完成
- ✅ 人机/随机/好友三模式独立入口

---

## 九、后续可选优化建议

1. 数据分析页增加「本周 / 本月」切换
2. 学习正确率折线图、对战胜率趋势
3. 对战记录筛选（好友/随机/人机）
4. 在数据看板添加“学习报告”入口

---
